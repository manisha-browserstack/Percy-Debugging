{
  "name": "percy-playwright-sample",
  "version": "1.0.0",
  "description": "Playwright + Percy sample for visual snapshot debugging (valid + broken 3rd-party assets)",
  "scripts": {
    "start": "node server.js",
    "test": "npx playwright test",
    "percy:test": "npx percy exec -- npx playwright test",
    "install-deps": "npm install"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@percy/cli": "^1.31.4",
    "@percy/playwright": "^1.0.0",
    "@playwright/test": "^1.40.0"
  }
}
